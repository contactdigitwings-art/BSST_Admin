## Packages
(none needed - using pre-installed framer-motion, lucide-react, recharts)

## Notes
- Tailwind Config: The design uses custom fonts (Outfit for display, Plus Jakarta Sans for body). Ensure they are loaded.
- Using standard pre-installed shadcn UI components alongside custom gradient components.
- The Admin / User routing is handled securely based on the `/api/me` response role.
